<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.chaossnow.ms.dao.URLDao">
    <!--namespace根据自己需要创建的的mapper的路径和名称填写-->
    <select id="listRolesByUser" resultType="String">
        SELECT p.url from admin_permission p where p.id = any(
            SELECT arp.pid from admin_role_permission arp where arp.rid = any(
                SELECT aur.rid from admin_user_role aur
                where aur.uid = any(SELECT u.id from user u where u.username = #{username})))
    </select>
</mapper>
